/*-------------------------------------------
  -- Hero
-------------------------------------------*/
#ts-hero {
  color: var(--color-white);
  display: flex;
  position: relative;
  overflow: hidden;

  .ts-background-image {
    background-position: top center;
  }
}